apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:

  - ../../apps/bundles/core/

  - config/

patches:
  - target:
      kind: StorageClass
      name: proxmox
    path: patches/csi-proxmox/zfs-device.yaml